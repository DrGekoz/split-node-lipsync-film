import React from 'react';
import {Composition,AbsoluteFill,Audio,Img,staticFile,useCurrentFrame,useVideoConfig} from 'remotion';
import data from '../test_props.json';
const Lip=({quote=false})=>{const f=useCurrentFrame(),{fps}=useVideoConfig();const t=f/fps;let state='M_idle';for(const c of data.cues){if(t>=c.start&&t<c.end){state=c.state;break;}}const ref=(quote?'quote/':'narration/')+(quote?data.refs[state].replace('narration_lip_sync','narration_quote_lip_sync'):data.refs[state]);return <AbsoluteFill><Img src={staticFile(ref)} style={{width:'100%',height:'100%',objectFit:'contain'}}/>{quote&&<Img src={staticFile('quote_card.png')} style={{position:'absolute',left:70,bottom:70,width:700,maxHeight:480,objectFit:'contain'}}/>}<Audio src={staticFile(data.audio)}/></AbsoluteFill>};
export const RemotionRoot=()=> <><Composition id="SplitNodeNarrationLipSync" component={Lip} durationInFrames={Math.ceil(data.duration*24)} fps={24} width={2560} height={1440}/><Composition id="SplitNodeNarrationQuoteLipSync" component={()=><Lip quote/>} durationInFrames={Math.ceil(data.duration*24)} fps={24} width={2560} height={1440}/></>;
